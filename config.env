CONFIG_ENV=config.env
# 1. 你的源码仓库 (刚才我们在Termux里推上去的那个)
KERNEL_SOURCE=https://github.com/Nancy804/android_kernel_oneplus_sm8150

# 2. 你的分支名 (刚才 git push 成功显示的那个)
KERNEL_SOURCE_BRANCH=lineage-19.1

# 3. 配置文件 (LineageOS SM8150 的标准配置)
KERNEL_DEFCONFIG=vendor/sm8150_defconfig

# 4. 生成的内核文件名 (SM8150 必须带 dtb)
KERNEL_IMAGE_NAME=Image.gz-dtb

# 5. 架构 (手机都是 arm64)
ARCH=arm64

# 6. 是否需要 KernelSU (如果你想要 KSU 就填 true，只想要墓碑就填 false)
# 这里我先填 true，反正有 KSU 更好用，不影响墓碑
USE_KERNELSU=true
KERNELSU_TAG=main

# 7. 编译器设置 (LineageOS 19.1 通常需要 Clang 12 或 13，这里让脚本自动选)
CLANG_VERSION=auto

# 8. 是否需要刷机包 (开启后会生成 AnyKernel3.zip，方便直接刷)
USE_ANYKERNEL3=true
